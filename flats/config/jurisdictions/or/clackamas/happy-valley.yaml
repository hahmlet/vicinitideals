label: Happy Valley
kind: city
eligible: true
notes: >-
  Ported from quadfit rules.yaml. Every value is `draft` -- quadfit's verification was against a coarser
  standard (one citation per row) and does not carry over. UNPORTED: orientation_constraint='entrance_only'.
  quadfit justified this in a YAML comment with no citation; it needs its own encoding rather than invented
  provenance.


  READ AND NOT ENCODED.


  (1) 16.42.080, shared outdoor recreation areas. Every residential table in 16.22 prints "400 sq.
  ft./unit provided in accordance with Section 16.42.080" against every district, which made it the
  loudest unfetched reference in the whole corpus -- eleven mentions, eight of them beside a number this
  screen uses. Fetched and read 2026-08-20. 16.42.080(A): "The standards of this section apply to
  subdivisions of 30 or more units." Four units is not thirty, so the row reaches somebody else's
  development. (B) excuses PUDs and multifamily of five or more as well, a second door out of the same
  standard and not the one this building uses. Sixteen hundred square feet of recreation tract that the
  tables appear to demand of a fourplex, and do not.


  (2) 16.42.030(B) is the part of that chapter that does reach the pod, and it is now encoded on every
  zone as min_landscaped_pct. Worth saying why the two land differently: 16.42.020 excuses quadplexes
  from 16.42.030(D), (F), (G), (H), (J) and (K), and (B) is not on the list.


  (4) NOT ENCODED AND SHOULD BE. Table 16.22.050 states the standards for the SFA, MUR-A and VTH
  districts and Table 16.22.060 for MUR-M and MUR-X. A quadplex is permitted by right in all three
  attached districts -- "Four-family dwelling (quadplex): P, P, P" -- and they are the densest
  residential land in the city, with lot minimums of 2,000 and 3,000 square feet against R-40's
  40,000. Fifteen footnotes are dismissed as scoped to those tables, and that dismissal is only
  honest while this item stands: encoding the districts returns all fifteen to the queue.


  (5) The townhome column. Every residential table in 16.22 prints a townhome row beside the row a
  quadplex is named in, and the townhome row is looser in four standards at once -- 1,500 square
  feet of lot against R-40's 40,000, no lot width at all, twenty feet of frontage against a
  hundred, and a zero interior side yard. All four are now carried as `unit_lots` variants. Before
  this pass the file held a lot the code permits this building on to a minimum twenty-six times the
  real one.


    (3) 16.63.020(F), density calculations. Fetched with 16.42 and read for the same reason -- 16.12's
  "net acre" and "floor area ratio" both defer to it, so the subtraction behind every measured_on in
  this layer lived in an unfetched chapter. Now cited directly. (F)(3) also lets dedicated right-of-way
  count toward minimum lot size, but only "in the case of a partition, where the City is requiring the
  dedication", which is a land-division proceeding rather than a lot fact and is not encoded.
ingest:
  geoid: null
  juris_city_codes:
  - HAPPY VALLEY
  zoning_layer: zoning_happy_valley
  zone_field: ZONE
  strip_lowercase_suffix: false
definitions:
  # "Corner lot. A lot bounded entirely by streets, or a lot which adjoins the
  # point of intersection of two or more streets and which the interior angle
  # formed by the extensions of the street lines in the direction which they
  # take at their intersections with lot lines other than street lines, forms
  # an angle of 135 degrees or less. In the event that any street line is a
  # curve at its point of intersection with a lot line other than street line,
  # the tangent to the curve at that point shall be considered the direction
  # of the street line."
  #
  # A 135-degree ceiling stated "or less", so it includes 135 -- the same
  # number Multnomah County writes as "less than 135" and excludes. Two
  # jurisdictions, one number, opposite answers on the boundary lot.
  #
  # The tangent sentence is not an extra rule for us, it is a measurement
  # convention we already follow: the angle is taken from each boundary side's
  # own bearing where it meets the next, which for a curved street line is its
  # tangent at that point.
  #
  # "Bounded entirely by streets" needs no separate encoding -- a lot with a
  # street on every side has intersecting frontages by any reading.
  corner_lot:
    test: intersecting_frontages
    count: 2
    max_intersection_angle_deg: 135.0
    quote: "or/clackamas/happy-valley/16.12.definitions.txt#L907-L908"
    cite: "HV LDC 16.12.030, Corner lot"

code:
- id: 16.12.definitions
  url: https://ecode360.com/print/HA4934?guid=43529904
  title: HV LDC Chapter 16.12 - Definitions
  # Prose about words, so almost nothing in it is a dimensioned standard and
  # the thinness guard reads it as a landing page. Read 2026-08-18: it is
  # 16.12.030, the definitions themselves, lot types set as a numbered list.
  allow_thin: true
- id: 16.22.residential
  url: https://ecode360.com/print/HA4934?guid=43529907
  title: HV LDC Chapter 16.22 - Residential Land Use Districts
- id: 16.42.landscaping
  url: https://ecode360.com/print/HA4934?guid=43529965
  title: HV LDC Chapter 16.42 - Landscaping, Fences and Walls, Recreation Areas
- id: 16.63.land-divisions
  url: https://ecode360.com/print/HA4934?guid=43529977
  title: HV LDC Chapter 16.63 - Land Divisions and Property Line Adjustments
zones:
  R40:
    section:
    - '16.22.020'
    cite_default:
      cite: HV LDC 16.22.020 Table 16.22.020-2 (Very Low Density)
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L160"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L271"
    max_density_du_per_acre:
      # Table 16.22.020-2 prints no ceiling against the quadplex row -- the
      # standard there is "Lot size (minimum and maximum density)", a lot area
      # rather than a rate, and it is already carried as min_lot_sqft. What the
      # table does state for a rate is one row lower: "Townhome maximum density
      # (units per net acre)", 4.4 du/net acre in R-40.
      #
      # So the base is exempt and the townhouse figure is the variant, the same
      # shape R-5 carries. The header row is quoted with the density row
      # because the table puts three districts on one printed line and 4.4
      # alone does not say which column it came from.
      #
      # 4.4 and not the 25 in R-5's table: each column states its own, and
      # reading one district's number across would put four times the real
      # ceiling on R-40.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L278,L281"
      # The acre this rate is per is a net one.
      # 16.12 defines a net acre as "one acre of developable land, as
      # calculated pursuant to Section 16.63.020(F)". That calculation is a
      # survey of the parcel, not an attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 4.4
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L278,L281"
    setback_front_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L290"
    setback_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L292"
      variants:
        # The cell prints `15/04` -- 15 feet, or zero, the trailing 4 being
        # footnote 4 run up against the number. The footnote is quoted with
        # the row because the row alone does not say what the zero is for.
        - value: 0
          # The footnote says townhomes, and a townhome in this code sits on
          # its own lot -- the table gives it a lot minimum two rows above the
          # one this setback belongs to. A party wall alone is not the
          # condition; the pair is, which is also how the other five zones and
          # Gladstone read the same sentence.
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L292,L305"
    setback_rear_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L291"
    setback_street_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L293"
    min_lot_sqft:
      value: 40000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L279"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 40,000 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L280"
    min_lot_width_ft:
      value: 100
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L282"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L282,L303"
    min_lot_depth_ft:
      value: 200
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L283"
    min_frontage_ft:
      # L286 is the "All other lots" row. The row above it is a cul-de-sac
      # exception, which is not encoded: whether a lot fronts a cul-de-sac is
      # not a fact this screen measures, and reading the looser row across
      # would pass lots the code holds to 100.
      value: 100
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L284,L286"
      variants:
        # The row below the general one: townhomes, twenty feet, in every
        # column of every residential table in this chapter. The comment this
        # replaces called it "a standard for a different product than the pod"
        # -- but the pod platted as townhouses is that product, which is how
        # the townhome density row three fields up is already read.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L287"
    max_coverage_pct:
      value: 20
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L288"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Setbacks/coverage from City Model Home Application form (LDC
      eff 3/19/2024). Height 45 ft front elev / 49 ft side+rear. Covered porch >=20% of bldg line cuts
      front to 15 ft. Quadplex by-right per HV Middle Housing amendments. min_lot is the nominal density
      basis (verify Table 16.22.020-2).'
  R20:
    section:
    - '16.22.020'
    cite_default:
      cite: HV LDC 16.22.020 Table 16.22.020-2
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L160"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L271"
    max_density_du_per_acre:
      # Table 16.22.020-2 prints no ceiling against the quadplex row -- the
      # standard there is "Lot size (minimum and maximum density)", a lot area
      # rather than a rate, and it is already carried as min_lot_sqft. What the
      # table does state for a rate is one row lower: "Townhome maximum density
      # (units per net acre)", 8.7 du/net acre in R-20.
      #
      # So the base is exempt and the townhouse figure is the variant, the same
      # shape R-5 carries. The header row is quoted with the density row
      # because the table puts three districts on one printed line and 8.7
      # alone does not say which column it came from.
      #
      # 8.7 and not the 25 in R-5's table: each column states its own, and
      # reading one district's number across would put four times the real
      # ceiling on R-40.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L278,L281"
      # The acre this rate is per is a net one.
      # 16.12 defines a net acre as "one acre of developable land, as
      # calculated pursuant to Section 16.63.020(F)". That calculation is a
      # survey of the parcel, not an attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 8.7
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L278,L281"
    setback_front_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L290"
    setback_side_ft:
      value: 10
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L292"
      variants:
        # The cell prints `10/04` -- 10 feet, or zero, the trailing 4 being
        # footnote 4 run up against the number. The footnote is quoted with
        # the row because the row alone does not say what the zero is for.
        - value: 0
          # The footnote says townhomes, and a townhome in this code sits on
          # its own lot -- the table gives it a lot minimum two rows above the
          # one this setback belongs to. A party wall alone is not the
          # condition; the pair is, which is also how the other five zones and
          # Gladstone read the same sentence.
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L292,L305"
    setback_rear_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L291"
    setback_street_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L293"
    min_lot_sqft:
      value: 20000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L279"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 20,000 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L280"
    min_lot_width_ft:
      value: 80
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L282"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L282,L303"
    min_lot_depth_ft:
      value: 100
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L283"
    min_frontage_ft:
      # L286 is the "All other lots" row. The row above it is a cul-de-sac
      # exception, which is not encoded: whether a lot fronts a cul-de-sac is
      # not a fact this screen measures, and reading the looser row across
      # would pass lots the code holds to 80.
      value: 80
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L284,L286"
      variants:
        # The row below the general one: townhomes, twenty feet, in every
        # column of every residential table in this chapter. The comment this
        # replaces called it "a standard for a different product than the pod"
        # -- but the pod platted as townhouses is that product, which is how
        # the townhome density row three fields up is already read.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L287"
    max_coverage_pct:
      value: 30
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L288"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Height 45 ft front / 49 ft side+rear. Porch >=20% bldg line
      -> front 15 ft. min_lot nominal.'
  R15:
    section:
    - '16.22.020'
    cite_default:
      cite: HV LDC 16.22.020 Table 16.22.020-2
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L160"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L271"
    max_density_du_per_acre:
      # Table 16.22.020-2 prints no ceiling against the quadplex row -- the
      # standard there is "Lot size (minimum and maximum density)", a lot area
      # rather than a rate, and it is already carried as min_lot_sqft. What the
      # table does state for a rate is one row lower: "Townhome maximum density
      # (units per net acre)", 11.6 du/net acre in R-15.
      #
      # So the base is exempt and the townhouse figure is the variant, the same
      # shape R-5 carries. The header row is quoted with the density row
      # because the table puts three districts on one printed line and 11.6
      # alone does not say which column it came from.
      #
      # 11.6 and not the 25 in R-5's table: each column states its own, and
      # reading one district's number across would put four times the real
      # ceiling on R-40.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L278,L281"
      # The acre this rate is per is a net one.
      # 16.12 defines a net acre as "one acre of developable land, as
      # calculated pursuant to Section 16.63.020(F)". That calculation is a
      # survey of the parcel, not an attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 11.6
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L278,L281"
    setback_front_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L290"
    setback_side_ft:
      value: 7
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L292"
      variants:
        # The cell prints `7/04` -- 7 feet, or zero, the trailing 4 being
        # footnote 4 run up against the number. The footnote is quoted with
        # the row because the row alone does not say what the zero is for.
        - value: 0
          # The footnote says townhomes, and a townhome in this code sits on
          # its own lot -- the table gives it a lot minimum two rows above the
          # one this setback belongs to. A party wall alone is not the
          # condition; the pair is, which is also how the other five zones and
          # Gladstone read the same sentence.
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L292,L305"
    setback_rear_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L291"
    setback_street_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L293"
    min_lot_sqft:
      value: 15000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L279"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 15,000 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L280"
    min_lot_width_ft:
      value: 70
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L282"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L282,L303"
    min_lot_depth_ft:
      value: 90
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L283"
    min_frontage_ft:
      # L286 is the "All other lots" row. The row above it is a cul-de-sac
      # exception, which is not encoded: whether a lot fronts a cul-de-sac is
      # not a fact this screen measures, and reading the looser row across
      # would pass lots the code holds to 60.
      value: 60
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L284,L286"
      variants:
        # The row below the general one: townhomes, twenty feet, in every
        # column of every residential table in this chapter. The comment this
        # replaces called it "a standard for a different product than the pod"
        # -- but the pod platted as townhouses is that product, which is how
        # the townhome density row three fields up is already read.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L287"
    max_coverage_pct:
      value: 35
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L288"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Height 45/49 ft. Porch >=20% bldg line -> front 15 ft. min_lot
      nominal.'
  R10:
    section:
    - '16.22.030'
    cite_default:
      cite: HV LDC 16.22.030 Table 16.22.030-2 (Low Density)
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L353"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L460"
    max_density_du_per_acre:
      # Table 16.22.030-2 prints no ceiling against the quadplex row -- the
      # standard there is "Lot size (minimum and maximum density)", a lot area
      # rather than a rate, and it is already carried as min_lot_sqft. What the
      # table does state for a rate is one row lower: "Townhome maximum density
      # (units per net acre)", 17.4 du/net acre in R-10.
      #
      # So the base is exempt and the townhouse figure is the variant, the same
      # shape R-5 carries. The header row is quoted with the density row
      # because the table puts three districts on one printed line and 17.4
      # alone does not say which column it came from.
      #
      # 17.4 and not the 25 in R-5's table: each column states its own, and
      # reading one district's number across would put four times the real
      # ceiling on R-40.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L468,L471"
      # The acre this rate is per is a net one.
      # 16.12 defines a net acre as "one acre of developable land, as
      # calculated pursuant to Section 16.63.020(F)". That calculation is a
      # survey of the parcel, not an attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 17.4
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L468,L471"
    setback_front_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L481"
    setback_side_ft:
      value: 7
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L483"
      variants:
        # The cell prints `7/04` -- 7 feet, or zero, the trailing 4
        # being the footnote run up against the number. The footnote is quoted
        # with the row because the row alone does not say what the zero is for,
        # and it says townhomes: "Interior side yard setbacks for townhomes may
        # be reduced to zero in compliance with applicable sections of the
        # adopted Uniform Building Code."
        - value: 0
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L483,L495"
    setback_rear_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L482"
    setback_street_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L485"
    min_lot_sqft:
      value: 10000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L469"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 10,000 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L470"
    min_lot_width_ft:
      value: 60
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L472"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L472,L493"
    max_coverage_pct:
      value: 40
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L479"
    min_lot_depth_ft:
      # Table 16.22.030-2, "Lot depth (minimum)". Stated by the code and absent
      # from this zone until now. Most Oregon codes are silent about depth, so
      # the field is optional and its absence raises nothing -- which is
      # exactly why a zone that does state it can go unencoded unnoticed.
      value: 80
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L473"
    min_frontage_ft:
      # Table 16.22.030-2, "Street frontage (minimum)", "All other lots" row.
      # Absent from this zone until now. The row above it, "Lots fronting on
      # cul-de-sac", states a smaller 35 feet; that one is not encoded because
      # whether a lot fronts a cul-de-sac is not a fact this screen measures,
      # and reading the looser row across would pass lots the code holds to
      # 50.
      value: 50
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L474,L476"
      variants:
        # The row below: townhomes, twenty feet in every column of every
        # residential table in this chapter.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L477"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Height 45/49 ft. Porch >=20% bldg line -> front 15 ft. min_lot
      nominal.'
  R8.5:
    section:
    - '16.22.030'
    cite_default:
      cite: HV LDC 16.22.030 Table 16.22.030-2
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L353"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L460"
    max_density_du_per_acre:
      # Table 16.22.030-2 prints no ceiling against the quadplex row -- the
      # standard there is "Lot size (minimum and maximum density)", a lot area
      # rather than a rate, and it is already carried as min_lot_sqft. What the
      # table does state for a rate is one row lower: "Townhome maximum density
      # (units per net acre)", 20.5 du/net acre in R-8.5.
      #
      # So the base is exempt and the townhouse figure is the variant, the same
      # shape R-5 carries. The header row is quoted with the density row
      # because the table puts three districts on one printed line and 20.5
      # alone does not say which column it came from.
      #
      # 20.5 and not the 25 in R-5's table: each column states its own, and
      # reading one district's number across would put four times the real
      # ceiling on R-40.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L468,L471"
      # The acre this rate is per is a net one.
      # 16.12 defines a net acre as "one acre of developable land, as
      # calculated pursuant to Section 16.63.020(F)". That calculation is a
      # survey of the parcel, not an attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 20.5
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L468,L471"
    setback_front_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L481"
    setback_side_ft:
      value: 5
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L483"
      variants:
        # The cell prints `5/04` -- 5 feet, or zero, the trailing 4
        # being the footnote run up against the number. The footnote is quoted
        # with the row because the row alone does not say what the zero is for,
        # and it says townhomes: "Interior side yard setbacks for townhomes may
        # be reduced to zero in compliance with applicable sections of the
        # adopted Uniform Building Code."
        - value: 0
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L483,L495"
    setback_rear_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L482"
    setback_street_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L485"
    min_lot_sqft:
      value: 8500
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L469"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 8,500 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L470"
    min_lot_width_ft:
      value: 50
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L472"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L472,L493"
    max_coverage_pct:
      value: 45
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L479"
    min_lot_depth_ft:
      # Table 16.22.030-2, "Lot depth (minimum)". Stated by the code and absent
      # from this zone until now. Most Oregon codes are silent about depth, so
      # the field is optional and its absence raises nothing -- which is
      # exactly why a zone that does state it can go unencoded unnoticed.
      value: 70
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L473"
    min_frontage_ft:
      # Table 16.22.030-2, "Street frontage (minimum)", "All other lots" row.
      # Absent from this zone until now. The row above it, "Lots fronting on
      # cul-de-sac", states a smaller 35 feet; that one is not encoded because
      # whether a lot fronts a cul-de-sac is not a fact this screen measures,
      # and reading the looser row across would pass lots the code holds to
      # 50.
      value: 50
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L474,L476"
      variants:
        # The row below: townhomes, twenty feet in every column of every
        # residential table in this chapter.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L477"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Layer value is ''R8.5'' (code text ''R-8.5''). Height 45/49
      ft. Porch >=20% -> front 15 ft. min_lot nominal.'
  R7:
    section:
    - '16.22.030'
    cite_default:
      cite: HV LDC 16.22.030 Table 16.22.030-2
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L353"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L460"
    max_density_du_per_acre:
      # Table 16.22.030-2 prints no ceiling against the quadplex row -- the
      # standard there is "Lot size (minimum and maximum density)", a lot area
      # rather than a rate, and it is already carried as min_lot_sqft. What the
      # table does state for a rate is one row lower: "Townhome maximum density
      # (units per net acre)", 24.9 du/net acre in R-7.
      #
      # So the base is exempt and the townhouse figure is the variant, the same
      # shape R-5 carries. The header row is quoted with the density row
      # because the table puts three districts on one printed line and 24.9
      # alone does not say which column it came from.
      #
      # 24.9 and not the 25 in R-5's table: each column states its own, and
      # reading one district's number across would put four times the real
      # ceiling on R-40.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L468,L471"
      # The acre this rate is per is a net one.
      # 16.12 defines a net acre as "one acre of developable land, as
      # calculated pursuant to Section 16.63.020(F)". That calculation is a
      # survey of the parcel, not an attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 24.9
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L468,L471"
    setback_front_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L481"
    setback_side_ft:
      value: 5
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L483"
      variants:
        # The cell prints `5/04` -- 5 feet, or zero, the trailing 4
        # being the footnote run up against the number. The footnote is quoted
        # with the row because the row alone does not say what the zero is for,
        # and it says townhomes: "Interior side yard setbacks for townhomes may
        # be reduced to zero in compliance with applicable sections of the
        # adopted Uniform Building Code."
        - value: 0
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L483,L495"
    setback_rear_ft:
      value: 22
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L482"
    setback_street_side_ft:
      value: 15
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L485"
    min_lot_sqft:
      value: 7000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L469"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 7,000 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L470"
    min_lot_width_ft:
      value: 50
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L472"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L472,L493"
    max_coverage_pct:
      value: 50
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L479"
    min_lot_depth_ft:
      # Table 16.22.030-2, "Lot depth (minimum)". Stated by the code and absent
      # from this zone until now. Most Oregon codes are silent about depth, so
      # the field is optional and its absence raises nothing -- which is
      # exactly why a zone that does state it can go unencoded unnoticed.
      value: 70
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L473"
    min_frontage_ft:
      # Table 16.22.030-2, "Street frontage (minimum)", "All other lots" row.
      # Absent from this zone until now. The row above it, "Lots fronting on
      # cul-de-sac", states a smaller 35 feet; that one is not encoded because
      # whether a lot fronts a cul-de-sac is not a fact this screen measures,
      # and reading the looser row across would pass lots the code holds to
      # 50.
      value: 50
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L474,L476"
      variants:
        # The row below: townhomes, twenty feet in every column of every
        # residential table in this chapter.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L477"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Height 45/49 ft. Porch >=20% -> front 15 ft. Coverage never
      binds a ~1,000 sf/unit fourplex footprint. min_lot nominal.'
  R5:
    section:
    - '16.22.040'
    min_density_du_per_acre:
      # Table 16.22.040-2, "Residential density (minimum)", R-5 column: None.
      # The zone states no floor, which is not a floor of zero -- it is the
      # absence of the test.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L647"
      # The acre this rate is per is a net one.
      # Table 16.22.040-2 states these per NET acre, which 16.12 defines as
      # "one acre of developable land, as calculated pursuant to Section
      # 16.63.020(F)". That calculation is a survey of the parcel, not an
      # attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
    max_density_du_per_acre:
      # No ceiling is printed for the quadplex on one lot. The townhome row is
      # a different standard for a different plat, carried as the variant.
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L646,L647"
      # The acre this rate is per is a net one.
      # Table 16.22.040-2 states these per NET acre, which 16.12 defines as
      # "one acre of developable land, as calculated pursuant to Section
      # 16.63.020(F)". That calculation is a survey of the parcel, not an
      # attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 25
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L646"
    cite_default:
      cite: HV LDC 16.22.040 Table 16.22.040-2 (Medium Density)
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L535"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L633"
    setback_front_ft:
      value: 20
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L658"
    setback_side_ft:
      value: 5
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L661"
      variants:
        # The cell prints `5/0 feet 3` -- 5 feet, or zero, the trailing 3
        # being the footnote run up against the number. The footnote is quoted
        # with the row because the row alone does not say what the zero is for,
        # and it says townhomes: "Interior side yard setbacks for townhomes may
        # be reduced to zero in compliance with applicable sections of the
        # adopted Uniform Building Code."
        - value: 0
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L661,L676"
    setback_rear_ft:
      value: 20
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L660"
    setback_street_side_ft:
      value: 8
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L662"
    min_lot_sqft:
      value: 7000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L644"
      variants:
        # The row under the one this value comes from: "Lot size (minimum):
        # Townhome1 -- 1,500 sq. ft." Four of them is 6,000, a figure the
        # table prints nowhere and the one that binds when the pod is platted
        # as townhouses. Encoded as the printed figure so the citation check
        # still compares against 1,500.
        #
        # The gap this closes is not small: 7,000 square feet against 6,000 is
        # the difference between a lot this city will never permit a
        # four-unit building on and one it will.
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L645"
    min_lot_width_ft:
      value: 40
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L648"
      variants:
        # Footnote 2 on the row: "Townhomes are exempt from the lot width
        # requirements." This table's copy adds a sentence: "Street
        # frontage controls lot width for townhomes." An exemption, not a smaller number -- the code
        # switches the standard off for this building rather than loosening it.
        - exempt: true
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L648,L675"
    max_coverage_pct:
      value: 60
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L656"
    min_lot_depth_ft:
      # Table 16.22.040-2, "Lot depth (minimum)". Stated by the code and absent
      # from this zone until now. Most Oregon codes are silent about depth, so
      # the field is optional and its absence raises nothing -- which is
      # exactly why a zone that does state it can go unencoded unnoticed.
      value: 60
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L649"
    min_frontage_ft:
      # Table 16.22.040-2, "Street frontage (minimum)", "All other lots" row.
      # Absent from this zone until now. The row above it, "Lots fronting on
      # cul-de-sac", states a smaller 35 feet; that one is not encoded because
      # whether a lot fronts a cul-de-sac is not a fact this screen measures,
      # and reading the looser row across would pass lots the code holds to
      # 40.
      value: 40
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L650,L652"
      variants:
        # The row below: townhomes, twenty feet in every column of every
        # residential table in this chapter.
        - value: 20
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L653"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: verified | Front 20 ft street-access / 10 ft alley-access. Height 45/49
      ft. Densest std SF zone. CORRECTED 2026-08-13 against Table 16.22.040-2: quadfit encoded the
      single-family tiers (5,000 sf lot / 50% coverage); the table splits by housing type and the
      quadplex tier is 7,000 sf min lot (note 8) and 60% coverage (Duplex, triplex, quadplex, townhome
      row). Townhome path is 1,500 sf/lot if platted as four lots.'
  MURS:
    section:
    - '16.22.040'
    min_density_du_per_acre:
      # Table 16.22.040-2, "Residential density (minimum)", MUR-S column: 6
      # du/net acre, and 16.22.040.A.2 states the same figure in prose. Four
      # units clear it up to 29,040 sq ft of lot and miss it above that, where
      # the district wants more of them than the pod has. Note 1 on the row
      # reaches neither half of the pod: it sends single-family detached
      # density to 16.63.020(F) and asks cottage clusters for four units per
      # gross acre.
      value: 6
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L647"
      # The acre this rate is per is a net one.
      # Table 16.22.040-2 states these per NET acre, which 16.12 defines as
      # "one acre of developable land, as calculated pursuant to Section
      # 16.63.020(F)". That calculation is a survey of the parcel, not an
      # attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
    max_density_du_per_acre:
      exempt: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L646,L647"
      # The acre this rate is per is a net one.
      # Table 16.22.040-2 states these per NET acre, which 16.12 defines as
      # "one acre of developable land, as calculated pursuant to Section
      # 16.63.020(F)". That calculation is a survey of the parcel, not an
      # attribute of it.
      measured_on:
        fact: net_developable_area
        # This city's own subtraction list, and it took two documents to
        # reach. 16.12 defines a net acre as "one acre of developable land,
        # as calculated pursuant to Section 16.63.020(F)" -- a glossary entry
        # that names the quantity and sends the reader somewhere else, which
        # is not a definition of what comes off.
        #
        # 16.63.020(F)(1) is where it is actually said. Constrained land is
        # public facilities in a tract including stormwater treatment, public
        # and private right-of-way, land under the Steep Slopes Overlay
        # (16.32), land under the Natural Resources Overlay (16.34), and
        # easement land the Planning Official finds similar. The quote is
        # that list rather than the glossary line, because the glossary line
        # answers no question a screen can ask.
        cite: HV LDC 16.63.020(F)(1), by way of 16.12 "Net acre"
        quote: "or/clackamas/happy-valley/16.63.land-divisions.txt#L133,L137,L141,L145,L149,L153"
      variants:
        - value: 25
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L646"
    cite_default:
      cite: HV LDC 16.22.040 (MUR-S, Mixed Use Residential-Single-Family); allows detached SFD
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    min_lot_sqft:
      # Table 16.22.040-2, "Lot size (minimum): Quadplex and cottage cluster",
      # MUR-S column -- 7,000 sq ft, the same figure R-5 carries and the row
      # this building is named in.
      #
      # Absent from this zone entirely until now, which is a standard the code
      # states and the screen did not test. Most of the MUR-S column reads
      # "Variable" and is genuinely unfixed; this cell is not one of them.
      value: 7000
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L642,L644"
      variants:
        - per_dwelling: 1500
          when: [unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L645"
    quadplex_allowed:
      value: true
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L535"
      variants:
        # The note over this use table, in every one of the three it appears
        # in: "Applies to a parent lot. Duplexes, triplexes, quadplexes, and
        # cottage clusters are not permitted on a child lot (i.e., previously
        # subdivided lot from a middle housing land division)."
        #
        # The permission itself is qualified, which is the strongest kind of
        # condition there is -- on a child lot this building is not a smaller
        # building, it is no building. The fact is assumed false because a
        # middle housing land division is rare and the answer is in the plat
        # record; the assumption comes back with the answer rather than
        # disappearing into it.
        - value: false
          when: [middle_housing_child_lot]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L633"
    setback_front_ft:
      value: 20
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L658"
    setback_side_ft:
      value: 5
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L661"
      variants:
        # The cell prints `5/0 feet3,4` -- 5 feet, or zero, the trailing 3
        # being the footnote run up against the number. The footnote is quoted
        # with the row because the row alone does not say what the zero is for,
        # and it says townhomes: "Interior side yard setbacks for townhomes may
        # be reduced to zero in compliance with applicable sections of the
        # adopted Uniform Building Code."
        - value: 0
          when: [attached_wall, unit_lots]
          quote: "or/clackamas/happy-valley/16.22.residential.txt#L661,L676"
    setback_rear_ft:
      value: 20
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L660"
    setback_street_side_ft:
      value: 8
      quote: "or/clackamas/happy-valley/16.22.residential.txt#L662"
    min_landscaped_pct:
      # LDC 16.42.030(B) states the landscaping requirement by housing type,
      # and (B)(1) names this building: "Single-family detached, single-family
      # attached dwellings, duplexes, triplexes and fourplexes: 20%."
      #
      # Not a zone standard and not in the district tables at all -- it is
      # citywide, in the design-standards chapter, which is why nine zones
      # carry the same figure from the same sentence.
      #
      # It reaches a quadplex. 16.42.020 excuses "quadplexes" from Section
      # 16.42.030(D), (F), (G), (H), (J) and (K); (B) is not on that list, and
      # naming the exceptions is how a code says the rest applies.
      #
      # The denominator is "gross developable lot area of the greater proposed
      # development", which is not the lot and is defined nowhere in 16.12 --
      # the glossary's two developable-area terms both defer to 16.63.020(F).
      # Left on the lot's own area deliberately: developable area is never more
      # than the lot, so twenty percent of it is never more square feet than
      # twenty percent of the lot, and the check as run asks for at least as
      # much as the code does. The screen already flags this one optimistic for
      # a bigger reason -- 16.42.030(A)(8) says driveways and parking do not
      # count as landscaped area and nothing here knows how much of the
      # leftover they take.
      value: 20
      cite: HV LDC 16.42.030(B)(1)
      url: https://ecode360.com/print/HA4934?guid=43529965
      retrieved: '2026-08-20'
      quote: "or/clackamas/happy-valley/16.42.landscaping.txt#L89,L93"
    notes: 'quadfit confidence: needs_verification | Layer value ''MURS'' = code ''MUR-S''. Setbacks from
      Model Home form (verified); max_coverage not shown in form (verify Table 16.22.040-2). Front 20
      street/10 alley. In HV middle-housing zone list.'
  R20CC:
    section:
    - '16.22.020'
    cite_default:
      cite: Assumed R-20 variant (unexplained 'CC' suffix in layer)
      url: https://ecode360.com/print/HA4934?guid=43529907
      retrieved: '2026-07-28'
    like: R20
    notes: 'A zoning-layer code, not a zone in LDC 16.22 — Happy Valley''s own chapter names R-40,
      R-20 and R-15 and nothing called R20CC, and what the CC suffix means is unconfirmed. The six
      standards here were a hand copy of R-20 carrying no citation, which is every Happy Valley row
      in the gap ledger. Adopted by reference instead, so each value cites the Table 16.22.020-2 cell
      R-20 was read from and an amendment to the table moves this code with it. The inference that
      the base is R-20 is the part that still needs a person, which is why it stays in the cite where
      a reviewer meets it rather than dissolved into six numbers that look sourced. If CC turns out
      to modify a standard, that standard belongs here as a local value over the reference.
      quadfit confidence: needs_verification.'
